import styled from 'styled-components';

const CardListWrapper = styled.ul`
  display: flex;
  flex-wrap: wrap;
  list-style: none;
  margin: 4px 0 0 0;
  padding: 0;
`;

export default CardListWrapper;
